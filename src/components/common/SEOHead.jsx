/**
 * Metadata is emitted by the matching App Router server page. Keeping this
 * component as a no-op preserves the existing page markup and call sites.
 */
export default function SEOHead() {
  return null
}
